import Todo from "../models/todoModel.js";

// This cleans up the data before i start the test
// before((done) => {
//   Todo.deleteMany({})
//     .then(() => done()) // Call done() after successful deletion
//     .catch((err) => done(err)); // Call done(err) if there's an error
// });

// This cleans up the data after the test finishes
// after((done) => {
//   Todo.deleteMany({})
//     .then(() => done()) // Call done() after successful deletion
//     .catch((err) => done(err)); // Call done(err) if there's an error
// });
